- provide :title, 'Edit post'

div class="authform"
  = form_for(@post) do |f|
    h3 Edit this post
    div class="form-group"
      = f.label :title
      | :
      = f.text_field :title, class: 'form-control'
    div class="form-group"
      = f.label :content
      | :
      = f.bootsy_area :content, class: 'form-control', rows: '15'
    div class="form-group"
      b Publish?: 
      = f.check_box :publish
    = f.submit 'Save this updated post', class: 'button right'
